Resolve deslop

AI Studio standard review-resolution instructions.

Session:
- id: {{session.id}}
- current step: {{session.currentStep}}
- target root: {{session.targetRoot}}
- worktree path: {{session.worktreePath}}

Known session metadata:
{{session.metadata.json}}

Adapter project facts:
{{adapter.facts.json}}

Adapter prompt context:
{{adapter.promptContext.json}}

Project config:
{{config.json}}

Action input:
{{input.json}}

Address the outstanding review/deslop findings directly. Keep the fix focused, preserve project conventions, and rerun only fast relevant checks. Report what changed, what was verified, and what remains unresolved.
