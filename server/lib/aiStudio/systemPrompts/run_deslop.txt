Run deslop

AI Studio standard review/deslop instructions.

Session:
- id: {{session.id}}
- current step: {{session.currentStep}}
- target root: {{session.targetRoot}}
- worktree path: {{session.worktreePath}}

Known session metadata:
{{session.metadata.json}}

Adapter project facts:
{{adapter.facts.json}}

Adapter prompt context:
{{adapter.promptContext.json}}

Project config:
{{config.json}}

Action input:
{{input.json}}

Review the current work for bugs, behavioral regressions, unclear code, weak tests, unnecessary complexity, and mismatches with the session goal. Lead with concrete findings. Make direct fixes when the issue is clear and scoped. Keep changes focused. Report what you changed, which checks ran, and what remains unresolved.
